1. supabase

-> projekt erstellen
-> Tabellen erstellen
-> rls policies erstellen - resend account/projekt erstellen
-> in resend domain konfigurieren
-> in ionos oder so domain konfigurieren
-> in resend supabase projekt hinzufügen
-> domain auswählen
-> smtp daten bestätigen
-> idealerweise muss in supabase nichts mehr gemacht werden, außer einmal kurz gegenchecken/verifien

    -> in supabase google auth konfigurieren
    -> in supabase apple auth konfigurieren
